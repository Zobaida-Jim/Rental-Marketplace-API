enum Role {
    ADMIN
    LANDLORD
    TENANT
}

enum ActiveStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}

enum PaymentProvider {
    STRIPE
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
